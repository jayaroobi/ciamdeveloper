# Windows local config — copy to forgeops/config/env.local inside the VM after mount
# On Windows host: run .\forgeops\scripts\init-local-windows.ps1

# Repo on Windows host
LOCAL_REPO_WINDOWS=C:\ciam

# ForgeOps clone location (inside Multipass VM)
FORGEOPS_REPO=/home/ubuntu/forgeops

FORGEOPS_TAG=2026.2.1
FORGEOPS_FQDN=forgeops.example.com
FORGEOPS_ENV_NAME=my-env
FORGEOPS_NAMESPACE=my-namespace
FORGEOPS_CLUSTER_ISSUER=default-issuer

# VM name for Multipass
MULTIPASS_VM=forgeops-lab
